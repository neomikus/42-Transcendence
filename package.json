{
  "name": "transcendence",
  "version": "1.0.0",
  "description": "Proyecto fullstack con React + Vite (frontend) y Node.js + Express + PostgreSQL (backend)",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
